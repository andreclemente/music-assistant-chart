groups:
  - name: Music Assistant Configuration
    description: Configure Music Assistant
#  - name: User and Group Configuration
#    description: Configure User and Group for Music Assistant
  - name: Network Configuration
    description: Configure Network for Music Assistant
#  - name: Storage Configuration
#    description: Configure Storage for Music Assistant
#  - name: Resources Configuration
#    description: Configure Resources for Music Assistant

questions:
  - variable: image.repository
    label: "Image Repository"
    group: Music Assistant Configuration
    description: "The repository for the Music Assistant image."
    schema:
      type: string
      required: true
  - variable: image.tag
    label: "Image Tag"
    group: Music Assistant Configuration
    description: "The tag for the Music Assistant image."
    schema:
      type: string
      default: "latest"
  - variable: image.pullPolicy
    label: "Image Pull Policy"
    group: Music Assistant Configuration
    description: "The pull policy for the image."
    schema:
      type: string
      default: "IfNotPresent"
      enum:
        - value: "IfNotPresent"
          description: "Only pull if not present"
        - value: "Always"
          description: "Always pull"
        - value: "Never"
          description: "Never pull"
  - variable: interface
    label: "External Interface"
    group: Network Configuration
    description: "Select the external interface for Music Assistant."
    schema:
      type: string
      required: true
